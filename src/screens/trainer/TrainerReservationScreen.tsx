import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const TrainerReservationScreen = () => {
  return (
    <SafeAreaView>
      <Text>트레이너 예약 전체조회 페이지</Text>
    </SafeAreaView>
  );
};
export default TrainerReservationScreen;
