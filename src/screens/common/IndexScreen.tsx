import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const IndexScreen = () => {
  return (
    <SafeAreaView>
      <Text>처음 시작 페이지 회원, 트레이너, 관리자 로그인 페이지</Text>
    </SafeAreaView>
  );
};
export default IndexScreen;
