import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const ProfileScreen = () => {
  return (
    <SafeAreaView>
      <Text>프로필 수정하는 페이지</Text>
    </SafeAreaView>
  );
};
export default ProfileScreen;
